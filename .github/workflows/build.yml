name: Build Windows Executable

on:
  push:
    branches: [main, new-version]
    tags:
      - 'v*'
  pull_request:
    branches: [main]
  workflow_dispatch:  # Allow manual trigger

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: 1.8.4
          virtualenvs-create: true
          virtualenvs-in-project: true
      
      - name: Install dependencies
        run: |
          cd clipper-cli
          poetry install --with dev
      
      - name: Build with PyInstaller
        run: |
          cd clipper-cli
          poetry run pyinstaller build.spec
      
      - name: Upload Windows executable
        uses: actions/upload-artifact@v4
        with:
          name: clipper-windows
          path: clipper-cli/dist/clipper.exe
          if-no-files-found: error
      
      - name: Create Release (on tag)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          files: clipper-cli/dist/clipper.exe
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-macos:
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: 1.8.4
          virtualenvs-create: true
          virtualenvs-in-project: true
      
      - name: Install dependencies
        run: |
          cd clipper-cli
          poetry install --with dev
      
      - name: Build with PyInstaller
        run: |
          cd clipper-cli
          poetry run pyinstaller build.spec
      
      - name: Upload macOS executable
        uses: actions/upload-artifact@v4
        with:
          name: clipper-macos
          path: clipper-cli/dist/clipper
          if-no-files-found: error
      
      - name: Create Release (on tag)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          files: clipper-cli/dist/clipper
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-linux:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: 1.8.4
          virtualenvs-create: true
          virtualenvs-in-project: true
      
      - name: Install dependencies
        run: |
          cd clipper-cli
          poetry install --with dev
      
      - name: Build with PyInstaller
        run: |
          cd clipper-cli
          poetry run pyinstaller build.spec
      
      - name: Upload Linux executable
        uses: actions/upload-artifact@v4
        with:
          name: clipper-linux
          path: clipper-cli/dist/clipper
          if-no-files-found: error
      
      - name: Create Release (on tag)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          files: clipper-cli/dist/clipper
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
